[project]
name = "server"
version = "0.1.0"
description = "FRED MCP Server"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]",
    "httpx",
    "pydantic",
    "python-dotenv",
    "uvicorn",
]
